git pull https://github.com/HarithHaroon/alx-zero_day.git
cd alx-zero_day
touch up_to_date
echo "touch up_to_date" > up_to_date
echo "echo "touch up_to_date" > up_to_date" > up_to_date
echo "echo "echo "touch up_to_date" > up_to_date" > up_to_date" > up_to_date
git add up_to_date
git commit -m "How to be up to date in git"
git push origin master
